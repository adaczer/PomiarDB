@model ModalTut.ViewModel.RegisterViewModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Register</title>
</head>
<body>
    <div>

        @using (@Html.BeginForm("Register", "Account", FormMethod.Post))
        {

            @Html.AntiForgeryToken()

            <div id="login-error">
                @Html.ValidationMessage("loginerror","")
                @Html.ValidationSummary()
            </div>

            <table id="login-table" style="margin-left:auto; margin-right:auto">
                <tr>
                    <td>
                        @Html.LabelFor(m => m.Email)
                    </td>
                    <td>
                        @Html.TextBoxFor(m => m.Email)
                        <div class="login-validation-message">
                            @Html.ValidationMessageFor(o => o.Email)
                        </div>
                    </td>
                </tr>
                <tr>

                    @*<tr>
                <td>
                    @Html.LabelFor(m => m.Login)
                </td>
                <td>
                    @Html.TextBoxFor(m => m.Login)
                    <div class="login-validation-message">
                        @Html.ValidationMessageFor(o => o.Login)
                    </div>
                </td>
            </tr>*@
                <tr>
                    <td>
                        @Html.LabelFor(o => o.Password)
                    </td>
                    <td>
                        @Html.PasswordFor(o => o.Password)
                        <div class="login-validation-message">
                            @Html.ValidationMessageFor(o => o.Password)

                        </div>
                    </td>
                    <td></td>
                </tr>

                @*<div class="form-group">
            @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
            </div>
        </div>

        <tr>
            <td>@Html.</td>
        </tr>*@


                <tr>
                    <td>
                        @Html.LabelFor(o => o.ConfirmPassword)
                    </td>
                    <td>
                        @Html.PasswordFor(o => o.ConfirmPassword)
                        <div class="login-validation-message">
                            @Html.ValidationMessageFor(o => o.ConfirmPassword)

                        </div>
                    </td>
                    <td></td>
                </tr>


                <tr>
                    <td colspan="3">
                        <input type="submit" value="Zarejestruj" style="height: 40px;" />
                    </td>
                </tr>



            </table>


        }


    </div>
</body>
</html>
